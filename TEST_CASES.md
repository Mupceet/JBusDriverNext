# Stale-While-Revalidate 手动测试用例

> TTL 已设为测试值：首页/演员 10s，类型 15s，翻页 30s
> 测试完成后记得改回生产值（文件中搜索 `TODO: 测试值`）

---

## 前置条件

- Debug 构建已安装到设备
- 网络畅通
- 了解关键 UI 指示器：
  - **顶部进度条**（LinearProgressIndicator）：出现 = 后台正在刷新缓存
  - **Snackbar 弹窗**："有新數據" = 后台刷新完成但因用户滚动未自动应用

---

## 用例 1：冷启动（无缓存）

**操作：**
1. 清除 App 数据（设置 → 应用 → 清除数据），或首次安装
2. 打开 App，进入影片列表

**预期：**
- 显示全屏 loading 转圈
- 网络数据返回后列表出现
- 无顶部进度条（没有缓存可供先显示）
- 记录从点击到看到内容的时间（基准值）

---

## 用例 2：热启动（磁盘缓存命中）

**操作：**
1. 正常加载影片列表（确保有缓存）
2. 完全退出 App（滑掉最近任务）
3. 重新打开 App

**预期：**
- 列表**瞬间出现**（从磁盘缓存读取，不应有 loading 转圈）
- 列表顶部出现**细进度条**（isRevalidating = true）
- 几秒后进度条消失（后台网络请求完成，数据已更新）
- 整个过程用户可以正常滚动浏览

---

## 用例 3：缓存过期 + 自动刷新

**操作：**
1. 正常加载影片列表
2. 等待 **15 秒**（超过 10s 的 TTL）
3. 切换到演员 Tab，再切回影片 Tab（触发重新 loadFirstPage）

**预期：**
- 切回影片 Tab 时，**旧数据立即显示**（缓存虽过期但仍然先展示）
- 顶部出现进度条（过期缓存触发后台刷新）
- 网络返回后数据更新为最新内容

---

## 用例 4：演员列表缓存

**操作：**
1. 切换到演员 Tab，等待列表加载完成
2. 退出 App
3. 重新打开 App，切到演员 Tab

**预期：**
- 演员列表瞬间出现（磁盘缓存）
- 顶部进度条出现几秒后消失
- 数据无缝更新

---

## 用例 5：类型分类缓存（1 小时 TTL → 测试 15s）

**操作：**
1. 打开类型筛选面板（点击影片列表顶部的类型按钮）
2. 等类型列表加载完成后关闭面板
3. 等待 **20 秒**
4. 再次打开类型筛选面板

**预期：**
- 类型列表瞬间显示（从缓存）
- 后台静默刷新

---

## 用例 6：关联影片页面缓存

**操作：**
1. 点击某个类型标签（如"热门"），进入关联影片页面
2. 等列表加载完成后返回
3. 再次点击同一个类型标签

**预期：**
- 列表瞬间从缓存加载
- 顶部进度条出现后消失

---

## 用例 7：网络断开 + 有缓存

**操作：**
1. 正常加载影片列表（确保有缓存）
2. 开启**飞行模式**
3. 等待 15 秒（让缓存过期）
4. 切换 Tab 再切回影片列表

**预期：**
- 缓存数据正常显示（虽然已过期）
- 顶部进度条短暂出现后消失（网络请求失败）
- **不弹错误提示**（有缓存时后台刷新失败静默忽略）
- 列表可以正常浏览

---

## 用例 8：网络断开 + 无缓存

**操作：**
1. 清除 App 数据
2. 开启**飞行模式**
3. 打开 App

**预期：**
- 显示全屏 loading
- 然后显示错误页面（"載入失敗，請重試"）
- 可点击重试按钮

---

## 用例 9：下拉刷新

**操作：**
1. 正常加载影片列表（有缓存，列表已显示）
2. 下拉触发刷新

**预期：**
- PullToRefresh 指示器出现
- 强制跳过缓存，从网络获取新数据
- 新数据替换列表内容
- 无顶部进度条（forceRefresh 模式不触发 revalidate）

---

## 用例 10：后台刷新 + 用户滚动了列表

**操作：**
1. 正常加载影片列表
2. 滚动列表到中间位置（不在顶部）
3. 等待 15 秒（缓存过期）
4. 切换 Tab 再切回（触发 loadFirstPage）

**预期（如果实现了滚动位置检测）：**
- 旧数据先显示
- 后台刷新完成后显示 Snackbar："有新數據" + "刷新"按钮
- 点击"刷新" → 数据更新，列表滚回顶部

**注意：** 当前实现中 `isAtTopForFreshUpdates` 默认为 `true`，所以会直接自动应用。
要测试 pending 机制，需要在 MovieListScreen 中跟踪列表滚动位置并调用
`viewModel.setAtTopForFreshUpdates(false)`，类似 ForumThreadListScreen 的实现。

---

## 用例 11：Kill 进程后磁盘缓存存活

**操作：**
1. 正常加载影片列表和演员列表
2. 在开发者选项中"终止后台进程"，或 `adb shell am force-stop me.jbus.debug`
3. 重新打开 App

**预期：**
- 影片和演员列表从磁盘缓存**瞬间恢复**
- 后台自动刷新更新数据

---

## 用例 12：翻页缓存（第二页）

**操作：**
1. 加载影片列表，滚动到底部触发加载更多（第二页）
2. 退出 App 后重新打开
3. 再次滚动到底部

**预期：**
- 首页从缓存瞬间加载
- 滚动到底部时第二页从缓存加载（翻页 TTL 30s）
- 如果超过 30s，第二页会重新从网络获取

---

## 测试完成后的清理

搜索 `TODO: 测试值` 将所有 TTL 改回生产值：
```
MOVIE_LIST_FIRST_PAGE_MILLIS = 5 * 60 * 1_000L    // 5分钟
MOVIE_LIST_NEXT_PAGE_MILLIS = 30 * 60 * 1_000L    // 30分钟
ACTRESS_LIST_FIRST_PAGE_MILLIS = 10 * 60 * 1_000L // 10分钟
ACTRESS_LIST_NEXT_PAGE_MILLIS = 30 * 60 * 1_000L  // 30分钟
GENRE_CATEGORIES_MILLIS = 60 * 60 * 1_000L         // 1小时
MOVIE_BY_URL_FIRST_PAGE_MILLIS = 5 * 60 * 1_000L  // 5分钟
MOVIE_BY_URL_NEXT_PAGE_MILLIS = 30 * 60 * 1_000L  // 30分钟
```
